"""
Задача 1: Функция с типами и разными видами аргументов

Реализуйте функцию `quadratic_equation`, которая вычисляет значение квадратного многочлена по формуле:
    a * x**2 + b * x + c

Функция должна принимать:
- x (float) — только позиционный аргумент
- a (float) — только позиционный аргумент
- b (float, по умолчанию 0.0) — только позиционный аргумент
- c (float, по умолчанию 0.0) — только именованный аргумент

Добавьте корректные аннотации типов для всех аргументов и возвращаемого значения.

Примеры:
- quadratic_equation(2, 1) -> 4.0
- quadratic_equation(1, 2, 3, c=4) -> 9.0
- quadratic_equation(3, 1, c=5) -> 14.0

Args:
    x (float): Значение переменной x (только позиционный).
    a (float): Коэффициент при x^2 (только позиционный).
    b (float, optional): Коэффициент при x (только позиционный, по умолчанию 0.0).
    c (float, optional): Свободный член (только именованный, по умолчанию 0.0).

Returns:
    float: Значение многочлена.
"""


# Ваш код здесь
def quadratic_equation(
    x: float, a: float, b: float = 0.0, /, *, c: float = 0.0
) -> float:
    return a * x**2 + b * x + c
    pass
