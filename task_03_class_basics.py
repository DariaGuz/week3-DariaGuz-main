"""
Задача 3: Класс с полями и методом

Реализуйте класс `Person`, который:
- При инициализации принимает имя (name: str) и возраст (age: int)
- Сохраняет их как поля экземпляра
- Имеет метод `greet`, который возвращает строку вида "Привет, меня зовут {name} и мне {age} лет!"

Примеры:
    p = Person("Алиса", 23)
    assert p.name == "Алиса"
    assert p.age == 23
    assert p.greet() == "Привет, меня зовут Алиса и мне 23 лет!"

Args:
    name (str): Имя человека.
    age (int): Возраст человека.

Returns:
    None
"""


# Ваш код здесь
class Person:
    pass
